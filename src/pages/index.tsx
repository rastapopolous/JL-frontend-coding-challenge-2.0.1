import Layout from "@/components/Layout";
import TodosHome from "@/components/TodosHome";

export const Home = () => {
  return (
    <Layout>
      <TodosHome />
    </Layout>
  );
};

export default Home;
